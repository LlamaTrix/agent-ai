# Estas vars las puede inyectar el agente Python al lanzar el subprocess,
# pero también pueden vivir aquí para correr el MCP standalone (debug).
API_BASE_URL=https://tu-laravel.com/api
# API_TOKEN=
HTTP_TIMEOUT_MS=25000
